// GENERATED from browserbase-wiki/wiki/product-map.md — edit THERE, then re-copy.
// The curated routing layer: always in the system prompt (~2-3k tokens).
export const PRODUCT_MAP = '# Browserbase Product Map\n\n> Hand-curated routing layer: what each piece of the Browserbase ecosystem is for,\n> when to use it, and how to decide. **Review-gated** — the crawler never edits this\n> file; a human updates it when launches change the map.\n> Last reviewed: 2026-07-03.\n\n## The decision framework: do you even need a browser?\n\nAsk in this order:\n\n1. **Does a clean, affordable API exist for the data/action?** → Use the API. No browser.\n2. **Is it read-only retrieval of pages that render server-side?** → **Fetch API** (no JS execution, 5 MB limit) or **Search API** for web search results. Cheap, fast, token-efficient.\n3. **Does the page require JavaScript rendering, login, or interaction (click/fill/navigate)?** → A **browser session** — write it with **Stagehand**.\n4. **Is the task a multi-step goal you\'d rather describe than script?** → **Browserbase Agents** (managed, natural-language task → run) or a **Stagehand agent** (in your code, more control).\n5. **Do you want no code at all?** → **Director** (director.ai) — build/deploy browser automations from a prompt.\n\nRules of thumb: prefer the *lowest* rung that works; scripts (Stagehand `act`/`extract`/`observe`) beat agents for repeatable flows (cheaper, deterministic); agents beat scripts for one-off or highly variable tasks.\n\n## Products\n\n### Browserbase platform (core)\nCloud Chromium at scale. Concepts: **Sessions** (one browser, one run — create via SDK/API; keepAlive sessions must be explicitly released, `close()` alone doesn\'t end them), **Contexts** (persist cookies/auth across sessions — the right way to "stay logged in"), **Live View** (watch/embed a running session), **Session Replay/Recording** (recorded sessions, embeddable), **Downloads API**, **Proxies** (incl. residential), **Identity/CAPTCHA solving**, **Stealth** (advanced stealth is Enterprise-only), **Model Gateway** (routed LLM access for Stagehand), **Functions** (deploy automation serverlessly on BB infra, 15-min cap), **Observability** dashboards.\n**Use when:** anything needing a real browser you control programmatically.\n\n### Stagehand (SDK — the default way to write browser automation)\nAI-powered browser automation SDK (TS-first; v3 current; Python/Go/Java API surface exists). Primitives: **`act`** (natural-language action), **`extract`** (schema-validated data out, zod), **`observe`** (discover available actions), **`agent`** (multi-step autonomous execution in your code). Runs local or on Browserbase. Caching (ActCache/AgentCache) makes repeated flows cheap/deterministic.\n**Use when:** you\'re writing code and want resilient automation without brittle selectors. Docs: docs.stagehand.dev (v3).\n\n### Browserbase Agents (API, launched 2026-06-30)\nManaged agent runs: describe the task in natural language, POST to the Agents API, get the run\'s result — no code to deploy or maintain.\n**Use when:** you want outcomes, not automation code; variable/one-off tasks; you accept less step-level control than a Stagehand agent. Docs: `/platform/agents/*`.\n\n### Search & Fetch APIs (web data, no session)\n**Search:** fast, token-efficient web search results. **Fetch:** retrieve a URL through BB infra — raw, markdown, or schema\'d JSON output; **does not execute JavaScript**; 5 MB cap.\n**Use when:** retrieval-only workloads; the "you don\'t need a browser" verdict. Escalate to a session only when JS/interaction is required.\n\n### Browse CLI + browse.sh (skills ecosystem)\n**Browse CLI** (`browse`, ships from the stagehand monorepo): run browser automation from the terminal; skills are reusable, versioned automation recipes. **browse.sh**: the public skills catalog (~400+ skills, per-site tasks, each a markdown SKILL.md).\n**Use when:** a skill already exists for your site/task (check browse.sh first!), or you live in the terminal/agent-CLI world.\n\n### Director (director.ai)\nNo-code agent builder: prompt → working browser automation, deployable, with 1Password credential integration.\n**Use when:** non-developers, demos, or quick internal automations.\n\n### MCP server (mcp-server-browserbase)\nModel Context Protocol server exposing BB sessions + Stagehand primitives to MCP clients (Claude, Cursor, etc.).\n**Use when:** you want your AI assistant to drive cloud browsers directly.\n\n## Common pairings\n- Scrape 500 public product pages daily → **Fetch API** (no browser needed).\n- Fill a form behind a login → **Stagehand** (`act`) + **Context** for the auth.\n- "Book me a table / do this task" from an app → **Agents API**; in-code with control → **Stagehand agent**.\n- Persistent logged-in scraping → **Contexts** + Stagehand `extract`.\n- Watch/verify what an automation did → **Live View** (live) / **Session Replay** (after).\n- Terminal/agent workflow on a known site → **browse.sh skill** via Browse CLI.\n';
